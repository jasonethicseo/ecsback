version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "arn:aws:ecs:ca-central-1:971422701599:task-definition/aws-tdn-dev-test:7" # 본인 Task 명
        LoadBalancerInfo:
          ContainerName: "test-backend" # 본인 컨테이너 명
          ContainerPort: 8080
# hooks:
#   BeforeInstall:
#     - location: scripts/stop_old_task.sh
#       timeout: 300
#       runas: root
#   AfterInstall:
#     - location: scripts/start_new_task.sh
#       timeout: 300
#       runas: root
