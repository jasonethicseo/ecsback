version: 1.0

Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: aws-tdn-dev-test       # family 이름 또는 ARN
        ContainerName: test-backend            # 컨테이너 이름
        ContainerPort: 8080                    # 서비스 포트

        
# hooks:
#   BeforeInstall:
#     - location: scripts/stop_old_task.sh
#       timeout: 300
#       runas: root
#   AfterInstall:
#     - location: scripts/start_new_task.sh
#       timeout: 300
#       runas: root
