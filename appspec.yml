version: 1.0

Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: aws-tdn-dev-test       # env.ECS_TASK_DEFINITION_FAMILY 혹은 실제 ARN
        ContainerName: test-backend            # env.CONTAINER_NAME
        ContainerPort: 8080                      # 서비스가 리스닝하는 포트

# hooks:
#   BeforeInstall:
#     - location: scripts/stop_old_task.sh
#       timeout: 300
#       runas: root
#   AfterInstall:
#     - location: scripts/start_new_task.sh
#       timeout: 300
#       runas: root
